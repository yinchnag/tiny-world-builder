# TinyWorld — CodeSurf Workspace Memory
_Generated by dreaming daemon. Last consolidated: 2026-06-25. Do not hand-edit this file._

---

## Overview

TinyWorld is a browser-based voxel world builder with an embedded multiplayer MMO layer (worlds rooms), flight simulation, floating islands, and a roadmap toward a full sky-sea survival game ("Skybound"). Served app is a static build from `dist/` via `./publish.sh`, Netlify-hosted. CodeSurf edits auto-commit; **main auto-pushes to GitHub → Netlify prod** — branches do not guarantee isolation.

An autonomous production line (`plans/production-line/`) is shipping Wave 2 features (deadline 2026-06-29).

**Economy status as of 2026-06-25:** Full economy stack (9 slices, ~40 exploits caught/fixed) dark-launched and live. **EARN side is now PUBLIC** (commit def8238) — any signed-in player can sell harvested resources for Earned GOLD; owner accounts get a server-derived flat 2x multiplier. SPEND side remains gated behind `requireTinyverseAccess`. $TINYWORLD mint confirmed live on-chain (Token-2022); `TINYWORLD_TOKEN_MINT` now set in Netlify env.

Key PR/commit state:
- PR #65 (lobby population bots) — merged, live
- PR #66 (Stripe buy-GOLD) — merged; returns 503 until `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` set
- PR #87 (privilege-escalation fix: `profiles.email` → `user.email`) — merged, live
- PR #90 (economy preview integration) — merged to `main`; earn-side ungated
- PR #91 (home carousel: community `world_shares`) — merged, live
- commit def8238 — earn side public launch; 2x owner multiplier; starter worlds open to signed-in players
- commit a2f0eb9 — Safari WebGL crash fix (guarded `createRenderer` fallback in `engine/world/01-render-core.js`)
- commit 64940dd — USDC buy-flow removed from islands (Enter → play only; no buy/Value-USDC/Solana-Pay)
- commit 7c9c8d4 — `gold-spend.mjs` missing `requireTinyverseAccess` gate fixed
- commit 5913a42 — news item "Mine, hunt and fish for coins" live (contains a carry-over promise; see open threads)
- 238 tests pass on prod branch

---

## Durable Architecture Facts

**Source shape**
- Entry: `tiny-world-builder.html` — boot config, DOM scaffold, ordered `<script src>` tags
- Styles: `styles/tiny-world.css` (~5.2k lines)
- Modules: `engine/world/00-*.js` through `70-*.js` + `99-late-boot.js` (~40k LoC total)
- All engine modules are **plain classic scripts sharing one global scope** — load order matters; every top-level `const`/`let`/`function` must be unique across all files. A duplicate identifier throws `SyntaxError` and silently kills that module while others continue loading. Prefix module-local globals (e.g. `_sr`, `_fl`, `_la`). New modules expose via `window.__*` from an IIFE.
- `tools/check.js` (run via `npm test`) fails the build on duplicate top-level identifiers across `engine/world/*.js`
- Three.js **r128** pinned; vendor self-hosted under `vendor/three/`. Do not bump the version.
- No bundler, no npm runtime dependencies. Static checks: `npm test`. Dist: `npm run build` or `./publish.sh`

**World data model**
- `world[x][z]` — intent: `{ terrain, terrainFloors, kind, floors }`
- `cellMeshes['x,z']` — render: `{ tile: Group, object: Group|null }`
- Always mutate via `setCell(x, z, opts)` — never write `world[x][z]` directly outside init
- `userData.landing` guards drop-in animations — never remove these checks

**Materials**
- `M.*` materials are shared across many meshes — clone before mutating `.color`
- `disposeGroup(group)` disposes geometries but NOT materials; cloned per-instance materials (smoke) self-dispose on death

**Cameras**
- `orthoCam`, `softCam`, `persCam` coexist; `camera` is a swappable reference
- `togglePerspective()` / `setCameraMode()` write to all projections/positions

**LandscapeEngine**
- `LandscapeEngine.js` is a superseded monolith — `getHeight`/chunk-building live in `engine/landscape/*.js` mixins that override it. Edit the mixins, not the monolith.

**Build pipeline**
- Dev: `npm run dev` (:3000) serves source directly — no build step during iteration
- Prod: `./publish.sh` → `dist/` → Netlify auto-deploys; `vercel.json` + `netlify.toml` both point to `dist/`
- PartyKit server (`party/index.js`) deploys via `partykit deploy`, NOT `publish.sh`

**Economy — two currency rails, never conflate**
- **Allowance GOLD** — holdings-based, non-transferable weekly allowance; `gold_ledger_events` table
- **Earned GOLD (Coins)** — player-to-player transferable; earned via template sales, referrals, resource sell; `coin_balances`/`coin_ledger` tables; `lib/coins.mjs` credit/debit/transfer primitive (adversarially reviewed, advisory-lock race-safe)
- GOLD is non-withdrawable with no real-world value and no conversion path to $TINYWORLD — this is the premise that makes public farming safe; the news item in 5913a42 contradicts this (see open threads)

**Economy gate architecture**
- `requireTinyverseAccess(auth.user, origin)` in `lib/tinyverse-access.mjs` guards all SPEND surfaces after auth
- Allowlist widened without redeploy via `TINYVERSE_ACCESS_EMAILS` env var (comma-separated)
- Exception: `/api/admin/gold-payout` gated by `x-admin-secret` only — intentional admin path
- Exception: `stripe-webhook` gated by Stripe signature verification only
- **EARN side (resources-sell) is ungated** — any signed-in player can sell harvested resources
- `gold-spend.mjs` had a missing gate; fixed commit 7c9c8d4

**Public world access rules (as of earn-side launch)**
- `kind=starter && status=published` worlds are accessible to any signed-in player (play token issued)
- Non-starter, draft, and unpublished worlds remain invite-only or owner-only
- Anonymous visitors cannot observe or harvest — must be signed in
- Owner multiplier (2x) is server-derived from `auth.user.email` (verified); not spoofable via body
- `worldDto.ownerEmail` is now opt-in excluded by default (PII fix, earn-side launch)

**$TINYWORLD on-chain**
- Token-2022 mint: `7bK4jRMa3aY85wJMQEQqWsmY9mmrRT32AFWZ23cBpump` (6 decimals, ~1B supply, pump.fun)
- Program: `TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb`
- `lib/solana.mjs tokenSummaryForOwner` uses `getTokenAccountsByOwner` with `{mint}` filter (no programId) — works for Token-2022; verified on mainnet: payment wallet `Fg8q9...pDyGr` returns 7,364.53
- `TINYWORLD_TOKEN_MINT` now SET in Netlify env; `SOLANA_RPC_URL` still needs Helius/QuickNode (public RPC rate-limits)
- Holdings overlay on home page is EMPTY until players link wallets and a balance backfill runs

**Multiplayer message routing trap**
- New world-room message handlers must go into `onWorldMessage`, NOT the main `onMessage` (world rooms early-return past it). This trap shipped chat-emotes' emote relay dead in prod.

**Observer role capability (verified empirically)**
- Empty-token peers are role `observe`; observers CAN move, chat, emote, and render as peers. Only harvest is play+profile gated.

---

## Module Map

Modules 00–45 are the core builder. Modules 46–70 are worlds/MMO/Skybound layers:

- `46-mesh-terrain.js` — opt-in voxel-block surface sculptor; persisted under `tinyworld:meshTerrain:*`
- `46-worlds-universe.js` — PartyKit presence/join (`WS`); `WS.setPlayChrome(true)` adds `body.tw-worlds-play` on room enter
- `47-worlds-room.js` — server-authoritative MMO room; positions/hearts/nodes; 5s tick; signed-token join; Postgres flush
- `48-worlds-harvest-hud.js` — fish/meat/plants/ore economy HUD; resource add-only path
- `49-worlds-avatar-picker.js` — 3D voxel picker, presets, live per-attribute customizer
- `50-worlds-play-chat.js` — in-world chat; `/emote` commands intercepted here
- `51-worlds-bots.js` — dev-only local bots; NOT the prod lobby bots
- `53-voxel-avatar.js` — `window.makeVoxelAvatar`; replaces 2.5D sprite stripes
- `54-fly-down.js` — fly-down to planet surface (key J)
- `55-stargate.js` — stargate placement/warp (key G)
- `56-gate-transit.js` — gate-to-gate transit animation (key H)
- `57-poser-surface.js` — WASD+mouse-look on poser surface; `_sr`-prefixed state; `sampleWorld` height helper
- `58-lobby-presentation.js` — in-world jumbotron slide screen; PartyKit `present` slide-sync
- `61-tinyverse-race-track.js` — Tinyverse ground-surface perimeter rally loop; kart race HUD
- `62-cctv-truman.js` — render-to-texture B&W CRT/VHS monitor feeds; moving-subject tracking
- `63-cctv-placement.js` — CCTV camera placement; jumbotron cuts to hottest live feed
- `66-lobby-admin.js` — EDIT LOBBY god-admin unlock (`body.tw-admin-fulledit`); `tw-play-mode` vs `tw-worlds-play` dual-hide system
- `67-cctv-view.js` — `?view=cctv` world mode; ortho video-wall; headless browse has no WebGL
- `68-notifications.js` — join/leave/chat/bot toasts + web notifications; implicit-join seeding gotcha
- `70-world-remix.js` — client-side World Remix UI (EC2; merged)

**Netlify functions (economy backend)**
- `netlify/functions/resources-sell.mjs` — E5: converts harvested resources to Earned GOLD (plants/fish 1, meat 2, ore 3); UNGATED (earn-public); idempotent (`resource_sales` table; same-key+diff-body → 409); advisory lock + FOR UPDATE + nonneg CHECK; capped at MAX_COIN_AMOUNT
- `netlify/functions/world-remix.mjs` — EC2: remix transaction; coinRef uses fixed-length hash (not raw TEXT share ID, stays under 128-char limit)
- `netlify/functions/stripe-checkout.mjs` / `stripe-webhook.mjs` — Stripe buy-GOLD; 503 on missing `STRIPE_SECRET_KEY`; raw-body + `constructWebhookEvent` sig verification; idempotency guard
- `netlify/functions/gold-spend.mjs` — Allowance GOLD spend; `requireTinyverseAccess` gate added commit 7c9c8d4
- `netlify/functions/gold-payout.mjs` — admin endpoint; gated by `x-admin-secret` only (intentional)
- `netlify/functions/holders-top.mjs` — `GET /api/holders/top`; top 5 by `wallet_accounts.token_balance_atomic`; returns username/redacted-wallet + worldsPublished + badge counts; NEVER amounts/full-key/PII; CDN-cached `s-maxage=120`; EMPTY until `TINYWORLD_TOKEN_MINT` set + wallet links exist
- `lib/coins.mjs` — EC1 coin primitive (credit/debit/transfer); adversarially reviewed; advisory-lock race-safe
- `lib/referrals.mjs` — G1 referral system; anti-Sybil verified-wallet gate; atomic cap counter
- `lib/ai.mjs` — AI1 metered generation via OpenRouter; reserve-first spend in Earned GOLD
- `lib/tinyverse-access.mjs` — `requireTinyverseAccess` gate; hard-coded allowlist + `TINYVERSE_ACCESS_EMAILS` env override (no redeploy needed)

---

## Active Systems & Workflows

**Production line** (`plans/production-line/`)
- Pipeline per feature: SPEC (Codex) → BUILD (worktree) → adversarial REVIEW (Codex+Claude) → TEST → PR → MERGE → NEWS → TWEET → STATUS
- Model tiering: Opus = orchestrate + security-critical economy; Sonnet = most building; Haiku = content/mechanical; Codex/GPT = spec + graphics + red-team
- `status.json` — live machine state written after every stage
- `mission-control.html` (repo root) — private dashboard; view at `localhost:3000/mission-control.html`
- Tweet drafts in `plans/production-line/tweets/<ID>.md` — X/Twitter posting BLOCKED (no creds)
- `SPEC.md` — canonical economy spec; Codex-validated; confirmed: gold-payout gate intentional, gold-spend gate fixed, allowlist env-override added
- **Autonomous GitHub Action live** (`.github/workflows/production-line.yml`; hourly cron `:17`): each tick drafts news+tweets into `plans/production-line/drafts/`, refreshes `status.json`, surfaces next non-economy item. Economy changes blocked by `ECONOMY_PROTECTED`/`touchesEconomy` guard. Auto-BUILD step (`PRODUCTION_LINE_AUTONOMOUS=true` + `ANTHROPIC_API_KEY`) is OFF until owner enables it. Auto-publish/post NOT enabled.

**Economy earn-side (public)**
- Any signed-in player can sell harvested resources for Earned GOLD via `POST /api/me/resources/sell`
- Starter worlds (`kind=starter && status=published`) open to any signed-in player; play tokens issued; harvest scoped to these worlds
- Owner (and `TINYVERSE_ACCESS_EMAILS` list) get server-side 2x multiplier; multiplier is not configurable by clients
- `/rewards` page (`rewards.html`) live: Resources & GOLD panel (sell) + buy-GOLD packs ("coming soon" until Stripe keys)
- Owner seeded 5000 Earned GOLD to own profile for testing; builder UI exposes Share + List-as-template buttons

**Top Holders overlay (public home page)**
- `GET /api/holders/top` + hero overlay (`scripts/top-holders.js` + `.top-holders` in `landing.css`)
- Top 5 by on-chain balance; shows username or redacted wallet + worlds/badge counts; hides when empty
- Currently empty — will populate once `SOLANA_RPC_URL` is set to a real RPC and players link wallets

**Lobby population bots** (`tools/lobby-bots.mjs`, `npm run bots:lobby`)
- Spawns ambient NPC peers into live lobby world `tidewater-bay`; external Node CLI (Node 22+)
- LLM via OpenRouter (`OPENROUTER_API_KEY`); `:free` model enforced; graceful degrade on 401/429
- Per-bot `this.epoch` generation token guards scheduler chains across disconnect; auto-reconnect with capped exponential backoff+jitter
- `TW_ORIGIN` required for cold-room self-load; must read `world.welcome` (own `conn.id`) before chat to avoid self-echo
- Live creds in **main-repo `.env`** (gitignored), NOT worktree root

**AI bots** (`tools/ai-bots.mjs`, `npm run bots:ai`)
- Joins as real `play` peers; uses Claude Messages API/haiku (swappable); requires `openMode` + `grassCells` in `world.state`; do not conflate with lobby-bots

**Pixel-art SVG badge pattern**
- Format: 64×64 viewBox, `<rect>` elements only — NO `<path>`, NO gradients, NO text
- Built as JS string via `+` concatenation; variable naming `var SVG_*`
- 4 economy achievement badges designed (including `var SVG_FIRST_SALE`); pending integration into badge-award module

---

## Open Threads

- **OPEN DECISION (unresolved, flagged twice):** news item commit 5913a42 promises preview GOLD "carried over and translated into the final economy" — contradicts the "GOLD is valueless, no conversion" premise that makes public farming safe. Options: (A) soften the news copy to drop the carry-over line (matches stated model), or (B) keep it and add a per-account daily earn cap. Do not unilaterally rewrite.
- **Prospector campaign (3 owner decisions pending):** reseed richer worlds, "Early Preview World" labeling, award the existing-but-never-granted Prospector badge on a `resource_sales` row, promo banner. Awaiting owner choices.
- **Stripe keys** — `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` needed for live buy-GOLD; currently 503; set in Netlify env and GitHub Actions secrets for CI
- **X/Twitter posting** — tweet drafts accumulating in `plans/production-line/tweets/`; unblocked once X API creds added
- **Full spend-side public launch** — requires owner decision to remove/relax `requireTinyverseAccess` from spend endpoints (remix/templates/AI/transfers/coins)
- **`SOLANA_RPC_URL`** — needs Helius/QuickNode endpoint; public RPC rate-limits; required for live on-chain balance reads
- **Owner's personal holder wallet** — payment wallet (`Fg8q9...pDyGr`) is known; owner's personal holder wallet (for Allowance GOLD) is still unknown
- **Achievement badges** — 4 pixel-art SVG vars designed; need wiring into badge-award and UI display system (G2/G3 gamification)
- **Allowance GOLD payout E2.1** — fresh on-chain read in the weekly payout (D1 follow-up); pending `SOLANA_RPC_URL` and confirmed holder wallet
- **GOLD/referral economics sign-off** — owner review of D1 (wallet-held fresh-snapshot) and D2 (two-balance) decisions pending for go-live numbers
- **Production line Wave 2 deadline** — 2026-06-29; autonomous action loop live but auto-BUILD step still requires `PRODUCTION_LINE_AUTONOMOUS=true` + `ANTHROPIC_API_KEY` to be set
