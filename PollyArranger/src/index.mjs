// PollyArranger — entry point (Phase 0 stub)
//
// Tutorial note:
//   This is intentionally a stub. The real CLI grows phase by phase per
//   ../docs/06-roadmap.md. Phase 1 will wire in the registry store, the state
//   machine, the orchestrator loop, and the MockAdapter so that `npm start`
//   drives a sample registry through the item lifecycle.
//
//   The architecture this will assemble is in ../docs/01-architecture.md; the
//   behavior it must produce is the walkthrough in ../docs/05-workflow-walkthrough.md.
//
// Stack: Node.js ESM (no bundler, no build step) — consistent with the parent
// project's tools/*.mjs. See ../docs/06-roadmap.md Phase 0.

function main() {
  console.log('PollyArranger — orchestrator not yet implemented.');
  console.log('See PROGRESS.md for the current phase and where to start.');
}

main();
